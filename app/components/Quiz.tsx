import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Quiz = () => {
  return (
    <View>
      <Text>Quizzz</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Quiz;
